server:
  port: 8089
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/KarunVeeravalli/server.webmvc_config
          clone-on-start: true
          search-paths: '{application}'
#          default-label: main